# API Configuration
VITE_API_URL=http://localhost:3001/api

# Casper Network Configuration
VITE_CASPER_NETWORK=casper-test
VITE_CONTRACT_HASH=hash-xxxxxxxxxxxxx

# RPC Endpoint - Use the proxy API to avoid CORS issues
# In production (Vercel), use the proxy endpoint
VITE_CASPER_RPC_URL=/api/casper-rpc

# Application Configuration
VITE_APP_NAME="Casper DeFi Yield Optimizer"
VITE_APP_VERSION=1.0.0

# Optional: Analytics
# VITE_ANALYTICS_ID=your-analytics-id
